name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pytest

      - name: Run tests
        run: pytest -q

# Optional: protected integration job (commented out). If you want a job that performs a real
# SMTP send as part of CI, enable it and add repository secrets SMTP_HOST, SMTP_PORT, SMTP_USER,
# SMTP_PASS and FROM_EMAIL in the repo settings. Running real sends in CI is potentially costly
# and leaks data unless carefully constrained.
#
# integration:
#   runs-on: ubuntu-latest
#   needs: tests
#   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
#   steps:
#     - uses: actions/checkout@v4
#     - name: Setup Python
#       uses: actions/setup-python@v4
#       with:
#         python-version: '3.11'
#     - name: Install deps
#       run: |
#         python -m pip install --upgrade pip
#         if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
#     - name: Run manual email test (integration)
#       env:
#         SMTP_HOST: ${{ secrets.SMTP_HOST }}
#         SMTP_PORT: ${{ secrets.SMTP_PORT }}
#         SMTP_USER: ${{ secrets.SMTP_USER }}
#         SMTP_PASS: ${{ secrets.SMTP_PASS }}
#         FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
#       run: |
#         python data/models/tests/send_test_Email.py
name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.10, 3.11, 3.12]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install system dependencies
        run: |
          python -m pip install --upgrade pip
      - name: Install dependencies
        run: |
          if [ -f requirements.txt ]; then python -m pip install -r requirements.txt; fi
          if [ -f requirements-dev.txt ]; then python -m pip install -r requirements-dev.txt; fi
      - name: Run tests
        run: pytest -q
      - name: Run feature check
        run: python run_features_check.py
      - name: Quick train
        run: python data/models/src/train.py --quick
